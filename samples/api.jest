use web

app = web

fn home():
    return {
        message: "Jestlang Online",
        status: "running"
    }

fn user():
    return {
        id: params.id,
        name: "Lucas",
        language: "Jestlang"
    }

app.get("/", home)
app.get("/users/:id", user)

app.run(3000)
